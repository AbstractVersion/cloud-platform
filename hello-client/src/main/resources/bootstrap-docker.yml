## YAML Template.
---

spring:
  main:
    banner-mode: off
  cloud:
    config:
        uri: http://config-server:8888
  application:
    name: hello-client
  profiles:
    active: docker
server:
  port: 8081
  
